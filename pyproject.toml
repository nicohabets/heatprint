[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "heatprint-core"
version = "0.2.6"
description = "Weather-corrected heating analytics for any mix of gas, heat pump, electric and district heat. Pure-Python core used by the Heatprint Home Assistant integration."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [{ name = "Nico Habets" }]
keywords = ["degree-days", "energy-signature", "heat-pump", "home-assistant", "knmi", "pbl", "prism"]
classifiers = [
  "Development Status :: 2 - Pre-Alpha",
  "Intended Audience :: Developers",
  "License :: OSI Approved :: MIT License",
  "Programming Language :: Python :: 3 :: Only",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Topic :: Home Automation",
]
# The core deliberately has NO hard runtime dependencies (see docs/adr/0002).
dependencies = []

[project.optional-dependencies]
http = ["aiohttp>=3.9"]
dev = ["pytest>=8", "pytest-asyncio>=0.23", "ruff>=0.5", "mypy>=1.10"]

[project.urls]
Homepage = "https://github.com/nicohabets/heatprint"
Issues = "https://github.com/nicohabets/heatprint/issues"

[tool.setuptools.packages.find]
where = ["custom_components/heatprint"]
include = ["heatprint_core*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
# Same layout HACS installs: heatprint_core lives inside the integration folder.
pythonpath = ["custom_components/heatprint"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
ignore = ["E501"]

[tool.ruff.lint.isort]
known-first-party = ["heatprint_core"]

[tool.ruff.lint.per-file-ignores]
# sys.path must be set before heatprint_core / Home Assistant imports (HACS bundle).
"custom_components/heatprint/__init__.py" = ["E402", "I001"]
"custom_components/heatprint/core_api.py" = ["E402", "I001"]

[tool.mypy]
python_version = "3.11"
warn_unused_ignores = true
ignore_missing_imports = true
